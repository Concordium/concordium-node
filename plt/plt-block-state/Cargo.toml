[package]
name = "plt-block-state"
version = "0.1.0"
edition = "2024"

[features]
default = ["ffi"]
ffi = ["dep:libc"]

[dependencies]
plt-scheduler-types.workspace = true

concordium_base.workspace = true
sha2.workspace = true
thiserror.workspace = true
libc = { workspace = true, optional = true }

[dev-dependencies]
assert_matches.workspace = true
hex.workspace = true